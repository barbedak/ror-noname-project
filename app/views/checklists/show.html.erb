<p style="color: green"><%= notice %></p>
<div class="container">
  <%= render @checklist %>

  <div class="mt">
    <%= link_to "Редактировать", edit_checklist_path(@checklist), class: "button button-main button-show" %>
    <%= button_to "Удалить", @checklist, method: :delete, class: "button button-main button-delete" %>
  </div>
</div>

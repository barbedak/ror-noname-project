<%= form_with(model: series) do |form| %>
  <% if series.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(series.errors.count, "error") %> prohibited this series from being saved:</h2>

      <ul>
        <% series.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div style="display: inline-block;">
    <div class="mt title-sm">
      <%= form.label :name, 'Название', class: 'form-text-field' %>
      <%= form.text_field :name, size: "30" %>
    </div>
    <div class="mt mr-sm">
      <%= form.submit 'Добавить серию', class: 'button button-main' %>
    </div>
  </div>
<% end %>

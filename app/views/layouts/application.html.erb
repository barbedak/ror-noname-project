<!DOCTYPE html>
<html>
  <head>
    <title>Учет промывок</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div>
      <p><%= link_to 'Сотрудники', employees_path, class: 'button button-main'%></p>
      <p><%= link_to 'Оборудование', equipments_path, class: 'button button-main' %></p>
      <p><%= link_to 'Варки', brews_path, class: 'button button-main' %></p>
      <p><%= link_to 'Типы оборудования', typeequipments_path, class: 'button button-main' %></p>
      <p><%= link_to 'Должности', '/jobtitle', class: 'button button-main' %></p>
      <p><%= link_to 'Серии полупродукта', series_index_path, class: 'button button-main' %></p>
      <hr>
    </div>
    <div>

      <%= yield %>
    </div>
  </body>
</html>

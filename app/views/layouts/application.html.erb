<!DOCTYPE html>
<html>
  <head>
    <title>Учет промывок</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="wrapper">
      <div class="navbar">
        <div><%= link_to 'Варки', brews_path, class: 'button button-main' %></div>
        <div><%= link_to 'Оборудование', equipment_index_path, class: 'button button-main' %></div>
        <div><%= link_to 'Типы оборудования', equipment_types_path, class: 'button button-main' %></div>
        <div><%= link_to 'Должности', job_titles_path , class: 'button button-main' %></div>
        <div><%= link_to 'Сотрудники', employees_path, class: 'button button-main'%></div>
        <div><%= link_to 'Серии полупродукта', series_index_path, class: 'button button-main' %></div>
        <div class="mt-sm"><%= link_to 'Протоколы промывки', checklists_path, class: 'button button-main' %></div>
      </div>
      <div>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
